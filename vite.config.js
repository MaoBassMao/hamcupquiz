// vite.config.js (新規作成または編集)
import { defineConfig } from 'vite'

export default defineConfig({
  // ↓↓↓ この base の設定を追加または修正します ↓↓↓
  base: '/HamCupQuizApp1/' // 必ずスラッシュで囲む '/リポジトリ名/'
  // 他の設定があれば、それはそのまま残してください
})